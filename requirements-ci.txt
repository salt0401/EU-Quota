# Dependencies for the daily GitHub Actions scrape.
# Major versions are capped so an upstream release cannot silently change or
# break the daily publish; bump deliberately (run the test suite first).
requests>=2.28,<3
beautifulsoup4>=4.12,<5
lxml>=4.9,<7
pandas>=2.0,<3
openpyxl>=3.1,<4
